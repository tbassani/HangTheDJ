class Track {
  constructor(id, title, artists, artURL, duration) {
    this.id = id;
    this.title = title;
    this.artists = artists;
    this.artURL = artURL;
    this.duration = duration;
  }
}

export default Track;
